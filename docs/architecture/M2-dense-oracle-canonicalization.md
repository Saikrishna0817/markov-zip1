# M2 dense oracle and canonicalization

Implemented `DenseLu`, rectangular dense matrix utilities, residual checks, and reversible canonicalization. Complexity is O(n^3) factorization and O(n^2) solve; this path is intentionally small and independent.

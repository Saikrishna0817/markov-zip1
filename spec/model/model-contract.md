# Model contract v1

Validated models own canonical CSC data with strictly increasing row indices per column and finite nonzero values. Mathematical infinity is represented by BoundKind, not a large number. Names are nonempty and unique. Dimensions, bounds, objective data, and variable types must agree.

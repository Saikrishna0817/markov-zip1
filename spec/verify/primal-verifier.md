# Primal verifier contract v1

The verifier checks a candidate against the original validated model, independently recomputes Ax and c^T x+c0, reports all material violations, and never upgrades feasibility to optimality. M1 reports primal feasibility only.
